<%=render :partial=>"shared/simple_notice_message"%>

<div style="float: right">
<%=link_to new_resource_path, {:class => "btn btn-success"} do%>
	<i class ="icon-plus icon-white"></i>
	ファイル追加
<%end%>
</div>

<table class="table table-striped">
<%@files.each do |file|%>
	<tr>
		<td><%=link_to get_filename(file), canvas_path(:path =>file)%></td>
	</tr>
<%end%>
	
</table>
